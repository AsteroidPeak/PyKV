[project]
name = "pykv"
version = "0.1.0"
description = "A concurrent, persistent, single-node key-value store with a REST API"
requires-python = ">=3.9"

[project.optional-dependencies]
dev = ["pytest>=7.0"]
